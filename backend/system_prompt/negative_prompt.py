def negative_prompt_generation():
    """Returns a default negative prompt for image generation."""
    return "blurry, low quality, distorted, watermark, text, bad anatomy, poorly drawn, low resolution, out of frame, overexposed, underexposed, artifacts, incorrect proportions"
